"""
Pareeksha Bhavan Monitor — source package.

Sub-modules (to be implemented in later phases):
  config      — settings loaded from environment / .env
  scraper     — fetches notification pages
  pdf         — downloads and extracts PDF text
  matcher     — keyword matching logic
  notifier    — Telegram + email dispatch
  store       — reads/writes last_seen.json
"""
